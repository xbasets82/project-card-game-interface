class Rules {
  constructor(initialNumberCards,initialCrupierNumberCards) {
    this.initialNumberCards = initialNumberCards;
    this.initialCrupierNumberCards = initialCrupierNumberCards;
  }
}

export default Rules;
